gradio>=6.0.0
numpy
opencv-python
Pillow
trimesh>=4.0.0
scipy
pystray
pytz
networkx
lxml
svglib
reportlab
colormath
svgelements>=1.9.0
shapely>=2.0.0

# PyTorch - Install separately based on your GPU:
# 
# LATEST - NVIDIA RTX 50 series & All Modern GPUs (CUDA 13.0):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
#
# NVIDIA RTX 50 series (CUDA 12.8):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# NVIDIA RTX 40/30/20 series (CUDA 12.6 - Recommended):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#
# NVIDIA RTX 40/30/20 series (CUDA 12.1 - Legacy):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# NVIDIA GTX 10 series (CUDA 11.8 - Older GPUs):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# For AMD GPUs - ROCm 6.3 (Linux only, for RX 6000/7000 series):
# NOTE: ROCm support is implemented but NOT tested by developers (no AMD hardware available)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.3
#
# For CPU only (no GPU):
# pip install torch torchvision
#
# Or use start.bat (Windows) which auto-detects and installs the appropriate version
